info:
  name: Buscar matéria por ID
  type: http
  seq: 8
  tags:
    - atividade-04

http:
  method: GET
  url: "{{baseUrl}}/subjects/1"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Retorna a matéria solicitada", function() {
          expect(res.status).to.equal(200);
          expect(res.body.success).to.equal(true);
          expect(res.body.data).to.have.property("professor");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
